<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="Ybridio.WinUI.Views.Ventas.VentasPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <Style TargetType="TabViewItem" BasedOn="{StaticResource OutlookTabItemStyle}"/>
    </Page.Resources>

    <!--
        Visual Container Hierarchy (Module Layer):
        Border wrapper con padding superior y fondo sutil para separación física clara
        del Workspace Layer. Evita efecto "tabs ensimados" y establece boundary visual.
    -->
    <Border Padding="16,12,16,16"
            Background="{ThemeResource LayerFillColorDefaultBrush}">

        <TabView x:Name="VentasTabs"
                 TabWidthMode="SizeToContent"
                 IsAddTabButtonVisible="False"
                 Background="Transparent"
                 SelectionChanged="VentasTabs_SelectionChanged">

            <TabViewItem x:Name="TabClientes" Header="Clientes" IsClosable="False">
                <Frame x:Name="FrameClientes"/>
            </TabViewItem>

            <TabViewItem x:Name="TabCotizaciones" Header="Cotizaciones" IsClosable="False">
                <Frame x:Name="FrameCotizaciones"/>
            </TabViewItem>

            <TabViewItem x:Name="TabPedidos" Header="Pedidos" IsClosable="False">
                <Frame x:Name="FramePedidos"/>
            </TabViewItem>

            <TabViewItem x:Name="TabVentas" Header="Ventas" IsClosable="False">
                <Frame x:Name="FrameVentas"/>
            </TabViewItem>

            <TabViewItem x:Name="TabOrdenesTrabajo" Header="Órdenes de Trabajo" IsClosable="False">
                <Frame x:Name="FrameOrdenesTrabajo"/>
            </TabViewItem>

        </TabView>

    </Border>
</Page>
