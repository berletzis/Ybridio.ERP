<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Ybridio.WinUI.Views.POS.PosPage"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:Ybridio.WinUI.Views.POS"
    xmlns:vm="using:Ybridio.WinUI.ViewModels.POS"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

	<Grid Padding="16" ColumnSpacing="16">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="340" />
		</Grid.ColumnDefinitions>

		<!-- CATÁLOGO -->
		<Grid Grid.Column="0">
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="*" />
			</Grid.RowDefinitions>

			<TextBlock Grid.Row="0"
                       Text="Mostrador — POS"
                       Style="{StaticResource TitleTextBlockStyle}"
                       Margin="0,0,0,12" />

			<AutoSuggestBox Grid.Row="1"
                            PlaceholderText="Buscar producto..."
                            QueryIcon="Find"
                            Text="{x:Bind ViewModel.Busqueda, Mode=TwoWay}"
                            Margin="0,0,0,12" />

			<ProgressBar Grid.Row="2"
                         VerticalAlignment="Top"
                         IsIndeterminate="True"
                         Visibility="{x:Bind ViewModel.IsLoading}"
                         Margin="0,0,0,8" />

			<GridView Grid.Row="2"
                      ItemsSource="{x:Bind ViewModel.Productos}"
                      SelectionMode="None">

				<GridView.ItemTemplate>
					<DataTemplate>
						<Border Padding="12" Width="160" Height="110">
							<StackPanel Spacing="4">

								<TextBlock Text="{Binding ProductoNombre}"
                                           TextWrapping="Wrap"
                                           MaxLines="2" />

								<TextBlock>
									<Run Text="Stock: " />
									<Run Text="{Binding Cantidad}" />
								</TextBlock>

								<Button Content="+ Agregar"
                                        Command="{Binding ElementName=Root, Path=ViewModel.AgregarAlCarritoCommand}"
                                        CommandParameter="{Binding}" />
							</StackPanel>
						</Border>
					</DataTemplate>
				</GridView.ItemTemplate>
			</GridView>
		</Grid>

		<!-- CARRITO -->
		<Grid Grid.Column="1">
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="*" />
				<RowDefinition Height="Auto" />
			</Grid.RowDefinitions>

			<TextBlock Text="Carrito"
                       Style="{StaticResource TitleTextBlockStyle}" />

			<ListView Grid.Row="1"
                      ItemsSource="{x:Bind ViewModel.Carrito}"
                      SelectionMode="None">

				<ListView.ItemTemplate>
					<DataTemplate x:DataType="vm:CartItemViewModel">
						<StackPanel Orientation="Horizontal" Spacing="8">

							<TextBlock Text="{x:Bind Nombre}" Width="120" />

							<!-- 🔥 FIX AQUÍ -->
							<NumberBox Value="{Binding Cantidad, Mode=TwoWay}"
                                       Width="60" />

							<TextBlock Text="{x:Bind Importe}" Width="80" />

							<Button Content="X"
                                    Command="{Binding ElementName=Root, Path=ViewModel.QuitarDelCarritoCommand}"
                                    CommandParameter="{Binding}" />
						</StackPanel>
					</DataTemplate>
				</ListView.ItemTemplate>
			</ListView>

			<Button Grid.Row="2"
                    Content="Registrar venta"
                    Command="{x:Bind ViewModel.VenderCommand}" />
		</Grid>
	</Grid>
</Page>