<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Ybridio.WinUI.Views.Inventario.InventarioPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <!-- Aplica OutlookTabItemStyle a todos los TabViewItem de esta página -->
        <Style TargetType="TabViewItem" BasedOn="{StaticResource OutlookTabItemStyle}"/>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ── Dashboard Light Strip ───────────────────────────────────────── -->
        <Border Grid.Row="0" Background="#FAFAFA" BorderBrush="#E5E5E5" BorderThickness="0,0,0,1" Padding="16,8">
            <StackPanel Orientation="Horizontal" Spacing="24">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <FontIcon Glyph="&#xE73F;" FontSize="14" Foreground="#737373"/>
                    <TextBlock Text="Inventario Operacional" FontSize="12" Foreground="#737373"/>
                </StackPanel>
                <!-- Indicadores dinámicos futuros: "X bajo stock", "Y salidas hoy", etc. -->
            </StackPanel>
        </Border>

        <!--
            Visual Container Hierarchy (Module Layer):
            Border wrapper con padding y fondo sutil para separación física clara
            del Workspace Layer. Evita efecto "tabs ensimados" y establece boundary visual.
        -->
        <Border Grid.Row="1"
                Padding="16,12,16,16"
                Background="{ThemeResource LayerFillColorDefaultBrush}">

            <!-- ── TabView ──────────────────────────────────────────────────────── -->
            <TabView x:Name="InventarioTabs"
                 TabWidthMode="SizeToContent"
                 IsAddTabButtonVisible="False"
                 Background="Transparent"
                 SelectionChanged="InventarioTabs_SelectionChanged">

            <!-- Existencias -->
            <TabViewItem x:Name="TabExistencias" Header="Existencias" IsClosable="False">

                <Frame x:Name="FrameExistencias" />
            </TabViewItem>

            <!-- Entradas -->
            <TabViewItem x:Name="TabEntradas" Header="Entradas" IsClosable="False">

                <Frame x:Name="FrameEntradas" />
            </TabViewItem>

            <!-- Salidas -->
            <TabViewItem x:Name="TabSalidas" Header="Salidas" IsClosable="False">

                <Frame x:Name="FrameSalidas" />
            </TabViewItem>

            <!-- Kardex -->
            <TabViewItem x:Name="TabKardex" Header="Kardex" IsClosable="False">

                <Frame x:Name="FrameKardex" />
            </TabViewItem>

            <!-- Conteo -->
            <TabViewItem x:Name="TabConteo" Header="Conteo" IsClosable="False">

                <Frame x:Name="FrameConteo" />
            </TabViewItem>

            <!-- Órdenes de Compra -->
            <TabViewItem x:Name="TabOrdenesCompra" Header="Órdenes de Compra" IsClosable="False">

                <Frame x:Name="FrameOrdenesCompra" />
            </TabViewItem>

            <!-- Productos -->
            <TabViewItem x:Name="TabProductos" Header="Productos" IsClosable="False">

                <Frame x:Name="FrameProductos" />
            </TabViewItem>

        </TabView>

        </Border>

    </Grid>
</Page>
