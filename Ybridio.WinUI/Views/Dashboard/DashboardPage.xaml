<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Ybridio.WinUI.Views.Dashboard.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d">

	<ScrollViewer Padding="24">
		<StackPanel Spacing="20">

			<TextBlock Text="Dashboard" FontSize="28" />

			<!-- Loading -->
			<ProgressRing IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
                          Width="40" Height="40" />

			<!-- Error -->
			<TextBlock Text="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}"
                       Foreground="Red" />

			<!-- KPIs -->
			<StackPanel Orientation="Horizontal" Spacing="16">

				<Border BorderThickness="1" Padding="16">
					<StackPanel>
						<TextBlock Text="Ventas del día"/>
						<TextBlock Text="{x:Bind ViewModel.VentasDelDia}"/>
						<TextBlock Text="{x:Bind ViewModel.TotalTransacciones}"/>
					</StackPanel>
				</Border>

				<Border BorderThickness="1" Padding="16">
					<StackPanel>
						<TextBlock Text="Caja"/>
						<TextBlock Text="{x:Bind ViewModel.CajaEstado}"/>
						<TextBlock Text="{x:Bind ViewModel.CajaDescripcion}"/>
					</StackPanel>
				</Border>

				<Border BorderThickness="1" Padding="16">
					<StackPanel>
						<TextBlock Text="Bajo stock"/>
						<TextBlock Text="{x:Bind ViewModel.BajoStock.Count}"/>
					</StackPanel>
				</Border>

			</StackPanel>

			<TextBlock Text="Últimas ventas" FontSize="18"/>

			<ListView ItemsSource="{x:Bind ViewModel.UltimasVentas}">
				<ListView.ItemTemplate>
					<DataTemplate>
						<StackPanel Orientation="Horizontal" Spacing="10">
							<TextBlock Text="{Binding Id}" />
							<TextBlock Text="{Binding Fecha}" />
							<TextBlock Text="{Binding Total}" />
						</StackPanel>
					</DataTemplate>
				</ListView.ItemTemplate>
			</ListView>

			<TextBlock Text="Productos con bajo stock" FontSize="18"/>

			<ListView ItemsSource="{x:Bind ViewModel.BajoStock}">
				<ListView.ItemTemplate>
					<DataTemplate>
						<StackPanel Orientation="Horizontal" Spacing="10">
							<TextBlock Text="{Binding ProductoNombre}" />
							<TextBlock Text="{Binding Cantidad}" />
						</StackPanel>
					</DataTemplate>
				</ListView.ItemTemplate>
			</ListView>

		</StackPanel>
	</ScrollViewer>
</Page>